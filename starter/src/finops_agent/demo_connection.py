from .budget_demo import BudgetDemo
from .harness import CopilotFinOpsHarness


def build_demo_harness(service: BudgetDemo) -> CopilotFinOpsHarness:
    # TODO(Lab 2): Connect the existing toolbox, scoped user tools, and instructions.
    # Import USER_INSTRUCTIONS from budget_demo, then follow the 5-line lab example.
    raise NotImplementedError(
        "Lab 2: connect CopilotFinOpsHarness in demo_connection.py"
    )
